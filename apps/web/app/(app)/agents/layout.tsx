import type { ReactNode } from 'react';
import '../../styles/agents.css';

/** Virtual agents (design/02): list and per-agent overview/configuration. */
export default function AgentsLayout({ children }: { children: ReactNode }) {
  return children;
}
