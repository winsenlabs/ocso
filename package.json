{
  "name": "ocso",
  "private": true,
  "description": "OCSO — Open Customer Service Orchestrator",
  "type": "module",
  "packageManager": "pnpm@11.1.2",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "vitest run --project unit",
    "test:int": "vitest run --project integration",
    "lint": "node scripts/check-source-guards.mjs"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:",
    "@types/node": "catalog:",
    "turbo": "^2.11.2",
    "vite": "catalog:"
  }
}
